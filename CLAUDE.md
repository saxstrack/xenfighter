# Project: Street Fighter MVP

## Python Virtual Environment
Always use the virtual environment at `.venv` for package installs and testing:
```bash
source /home/ss123/street-fighter-mvp/.venv/bin/activate
```
Use `pip install` only after activating this venv. Never install packages globally.
